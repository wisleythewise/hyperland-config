# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# default Monitor config

# *********************************************************** #
# 
# NOTE: This will be overwritten by NWG-Displays 
# once you use and click apply. You can still find this
# default at ~/.config/hypr/Monitor_Profiles/default.conf
#
# *********************************************************** #


# Monitor Configuration
# See Hyprland wiki for more details
# https://wiki.hyprland.org/Configuring/Monitors/
# Configure your Display resolution, offset, scale and Monitors here, use `hyprctl monitors` to get the info.

# Monitors
# Samsung LC34G55T ultrawide (desk monitor): LEFT at 0x0, laptop to its right.
# Matched by EDID description (NOT the port), so it works through any hub.
# Hubs may advertise a fake 3840x2160 "preferred" mode (16:9 on this 21:9
# panel), so force the real native mode 3440x1440. The hub also advertises
# 3440x1440@98.84 but that modeset gives a black screen over its link,
# so stay at 59.97 while going through this hub.
monitor = desc:Samsung Electric Company LC34G55T 0x9094A823, 3440x1440@59.97, 0x0, 1

# JRP7003 portable monitor (matched by its EDID name, NOT the port):
# its native 3440x1440 is only offered @50Hz and that modeset fails
# (green screen / no sync) over the current DP link, so force 1920x1080@60.
# Scale 2 per preference. Pinned to the LEFT at 0x0.
# If you get a better DP cable you can try 3440x1440@60 here.
monitor = desc:JRP JRP7003, 1920x1080@60, 0x0, 2

# Laptop panel, auto-placed to the right of whatever external is present
monitor = eDP-1, 2048x1280@120, auto, 1

# Any OTHER external monitor: use its own preferred (native) mode, auto-placed.
# This is why a different monitor is NOT capped to 1080p.
monitor=,preferred,auto,1

# NOTE: for laptop, kindly check notes in Laptops.conf regarding display
# Created this inorder for the monitor display to not wake up if not intended.
# See here: https://github.com/hyprwm/Hyprland/issues/4090

# Some examples to set your own monitor
#monitor = eDP-1, preferred, auto, 1
#monitor = eDP-1, 2560x1440@165, 0x0, 1 #own screen
#monitor = DP-3, 1920x1080@240, auto, 1 
#monitor = DP-1, preferred, auto, 1
#monitor = HDMI-A-1, preferred,auto,1

# QEMU-KVM, virtual box or vmware
#monitor = Virtual-1, 1920x1080@60,auto,1

# to disable a monitor
#monitor=name,disable

# Mirror samples
#monitor=DP-3,1920x1080@60,0x0,1,mirror,DP-2
#monitor=,preferred,auto,1,mirror,eDP-1
#monitor=HDMI-A-1,2560x1440@144,0x0,1,mirror,eDP-1

# 10 bit monitor support - See wiki https://wiki.hyprland.org/Configuring/Monitors/#10-bit-support - See NOTES below
# NOTE: Colors registered in Hyprland (e.g. the border color) do not support 10 bit.
# NOTE: Some applications do not support screen capture with 10 bit enabled. (Screen captures like OBS may render black screen)
# monitor=,preferred,auto,1,bitdepth,10

#monitor=eDP-1,transform,0
#monitor=eDP-1,addreserved,10,10,10,49

# workspaces - Monitor rules
# https://wiki.hyprland.org/Configuring/Workspace-Rules/
# SUPER E - Workspace-Rules 
# See ~/.config/hypr/UserConfigs/WorkspaceRules.conf

